export default function getNotificationLabel (eventType, data) {

    if (data.length > 1) {
        return "TBC";
    } else if (data.length === 1) {

        // handle event types differently
    } else {

        // handle event types differently
    }
}
